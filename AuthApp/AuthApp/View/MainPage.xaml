<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AuthApp"
             x:Class="AuthApp.MainPage"
              xmlns:ViewModels="clr-namespace:AuthApp.ViewModel;assembly=AuthApp">


    <ContentPage.BindingContext>
        <ViewModels:StudentViewModel />
    </ContentPage.BindingContext>
    
    <ListView ItemsSource="{Binding ListStudentList}" HasUnevenRows="False" x:Name="StudentListView">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Vertical">
                        <Label x:Name="ID" Text="{Binding Id}"/>
                        <Label x:Name="FullName" Text="{Binding Name}"/>
                        <Label x:Name="LastName" Text="{Binding LastName}"/>
                        <Label x:Name="Mobile" Text="{Binding Mobile}"/>
                        <Label x:Name="Image" Text="{Binding Image}"/>
                    </StackLayout>
                </ViewCell>

            </DataTemplate>
        </ListView.ItemTemplate>
        
    </ListView>

</ContentPage>
